# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
.pytest_cache/
env/
venv/
.env

# Build & Dist
build/
dist/
*.egg-info/

# AIC generated
.aic/
bench/.cache/
# bench/results.json is deliberately tracked: viz/export.py reads it to put
# the timings on the page. Ignoring it is how the page ended up with
# hand-typed numbers that drifted from the benchmark.
bench/surfaces.json

# System
.DS_Store

# Regenerated by viz/export.py from the pinned sdists.
# Tracked on purpose: it is the deliverable, and it needs no build step.
